Button.stories.mdx

import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { within, userEvent } from '@storybook/testing-library';

import Button from '.';

<Meta title="Button" component={Button} />

export const Template = ({ isDone }) => (
  <div className="w-48 h-48">
    <Button isDone={isDone}>Button</Button>
  </div>
);

<Canvas>
  <Story
    name="Button"
    args={{
      isDone: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>